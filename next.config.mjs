/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: "/app", // Somente se precisar de um caminho base específico
  trailingSlash: true, // Opcional: adiciona uma barra ao final das URLs
};

export default nextConfig;
